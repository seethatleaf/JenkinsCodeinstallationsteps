ls -l

# Fix Windows line endings (CRLF -> LF)
sed -i 's/\r$//' cleanup_release_files.sh

# Ensure correct shell
chmod +x cleanup_release_files.sh

# Run explicitly with bash (most reliable)
bash cleanup_release_files.sh
